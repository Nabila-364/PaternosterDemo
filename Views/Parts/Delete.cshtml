@model PaternosterDemo.Models.Part
@{
    ViewData["Title"] = "Verwijder Onderdeel";
}

<div class="container mt-5">
    <div class="card shadow-lg border-danger">
        <div class="card-header bg-danger text-white">
            <h4><i class="bi bi-exclamation-triangle"></i> Bevestig Verwijdering</h4>
        </div>
        <div class="card-body">
            <p>Weet je zeker dat je dit onderdeel wilt verwijderen?</p>
            <ul class="list-group mb-3">
                <li class="list-group-item"><strong>Naam:</strong> @Model.Name</li>
                <li class="list-group-item"><strong>Artikelnummer:</strong> @Model.ArticleNumber</li>
                <li class="list-group-item"><strong>Omschrijving:</strong> @Model.Description</li>
            </ul>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="PartId" />
                <button type="submit" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Verwijderen
                </button>
                <a asp-action="Index" class="btn btn-secondary">Annuleren</a>
            </form>
        </div>
    </div>
</div>
